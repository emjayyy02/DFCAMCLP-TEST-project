# Copy to .env. Generate a local password; do not commit the populated file.
APP_ENV=development
APP_URL=http://localhost:3000
BETTER_AUTH_URL=http://localhost:3000
# Generate a unique value with at least 32 characters. Never reuse this placeholder.
BETTER_AUTH_SECRET=
# Local-only password for the clearly fake .invalid seed accounts.
AUTH_SEED_PASSWORD=
POSTGRES_USER=portal_dev
POSTGRES_DB=portal_dev
POSTGRES_PASSWORD=
# Use the same password above; URL-encode special characters.
DATABASE_URL=
